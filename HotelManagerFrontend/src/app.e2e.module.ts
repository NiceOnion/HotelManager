import { NgModule } from '@angular/core';

@NgModule({
    imports: [
        // Import the necessary Angular modules and other dependencies
        // that your E2E tests require
    ],
    exports: [
        // Export any components, directives, or pipes that your E2E tests
        // need to access from the module
    ]
})
export class AppE2eModule { }
